[project]
name = "auto-check-homework"
version = "0.1.0"
description = "An automatic solution verification service in Python for programming assignments"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openpyxl>=3.1.5",
]
